{
  "description": "Core JSON RPC request.",
  "type": "object",
  "properties": {
    "jsonrpc": {
      "description": "Version indicator for the JSON-RPC request.",
      "type": "string",
      "default": "2.0"
    },
    "id": {
      "type": "string",
      "description": "An arbitrary string that will be returned with the response. May be omitted or set to an empty string (“”)"
    },
    "method": {
      "type": "string",
      "required": true,
      "description": "The RPC method name (e.g. getblock). See the RPC section for a list of available methods."
    },
    "params": {
      "type": "array",
      "items": {"type": "any"}
    }
  },
  "additionalProperties": false
}
