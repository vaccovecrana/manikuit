{
  "description": "Crypto gateway configuration source.",
  "type": "object",
  "properties": {
    "rootUrl": {"$ref": "mk-schema-types.json#/uri"},
    "username": {
      "type": "string",
      "description": "RPC endpoint username."
    },
    "password": {
      "type": "string",
      "description": "RPC endpoint password."
    },
    "ignoreSsl": {
      "type": "boolean",
      "description": "Whether to ignore SSL peer validation."
    }
  },
  "additionalProperties": false,
  "javaType" : "io.vacco.mk.base.HttpConfig"
}
