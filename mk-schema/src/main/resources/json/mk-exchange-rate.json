{
  "description": "An exchange rate between a crypto currency and a fiat currency.",
  "type": "object",
  "properties": {
    "crypto": {"$ref": "schema-types.json#/cryptoCurrency"},
    "fiat": {"type": "string", "minLength": 3},
    "last": {"type": "number"}
  },
  "required": ["cryptoCurrency", "fiatCurrency", "last"],
  "additionalProperties": false
}
