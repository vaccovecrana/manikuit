{
  "description": "core crypto gateway payment.",
  "additionalProperties": false,
  "properties": {
    "type": {"$ref": "schema-types.json#/cryptoCurrency"},
    "address": {"$ref": "schema-types.json#/cryptoAddress"},
    "cipherText": {
      "description": "AES GCM implementation specific encoded address private components (key data/passphrase, etc.).",
      "type": "string", "minLength": 16, "maxLength": 2048
    },
    "iv": {"type": "string", "maxLength": 256},
    "gcmKey": {"type": "string", "maxLength": 256},
    "txId": {"$ref": "schema-types.json#/txHash"},
    "amount": {"$ref": "schema-types.json#/encodedAmount"},
    "status": {"$ref": "schema-types.json#/paymentStatus"},
    "confirmations": {"$ref": "schema-types.json#/txConfirmationCount"}
  },
  "type": "object"
}
